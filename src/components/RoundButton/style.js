import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    borderRadius: 4,
    backgroundColor: "#488aff",
    padding: 8
  },
  title: {
    fontSize: 14,
    color: "#fff",
  },
});
